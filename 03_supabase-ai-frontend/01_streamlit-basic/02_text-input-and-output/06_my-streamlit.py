# 이미지 로딩 
# 여러가지 입력 창을 만들고 
# 마지막 까지 입력 하면 
# 화면에 입력 된 내용을 출력.


import streamlit as st  # Python 코드로 웹 화면을 만들기 위해 Streamlit을 st라는 별칭으로 가져옵니다.

st.title("오늘 음식을 평가해 보세요!")  # Streamlit 화면의 가장 큰 제목을 표시합니다.

name = st.text_input("평론가분 성함을 입력해 주세요.")  # 사용자가 입력한 이름 문자열을 name 변수에 저장합니다.

if name:  # name에 빈 문자열이 아닌 값이 들어 있으면 인사말을 표시합니다.
    st.write(f"안녕하세요, {name}님!")  # 입력받은 이름을 포함해 인사말을 화면에 출력합니다.
else:  # 아직 이름을 입력하지 않은 상태라면 안내 메시지를 표시합니다.
    st.warning("이름을 입력하면 인사말이 표시됩니다.")  # 주의가 필요한 상황을 경고 메시지로 표시합니다.

age = st.number_input("몇명에서 드셨나요?", min_value=0, max_value=120, value=20)  # 계산 결과나 입력값을 이후 코드에서 다시 쓰기 위해 변수에 저장합니다.
st.write(f"오늘은 {age}에서 드셨군요!.")  # 문자열, 숫자, 객체를 Streamlit 화면에 출력합니다.

language = st.selectbox("먹은 음식의 장르를 선택해 주세요", ["한식", "중식", "양식", "일식"])  # 계산 결과나 입력값을 이후 코드에서 다시 쓰기 위해 변수에 저장합니다.
st.write(f"음식 장르: {language}")  # 문자열, 숫자, 객체를 Streamlit 화면에 출력합니다.

if language == "Python" and level == "입문":  # 조건식이 True일 때만 아래 들여쓰기 블록을 실행합니다.
    st.info("음식은 사랑입니다.")  # 다음 행동을 안내하는 정보 메시지를 표시합니다.

food = st.food_input("드신 음식을 알려주세요!")
st.write(f"{food} 이군요!")

level = st.radio("해당 음식점은 몇점 인가요?", ["★", "★★", "★★★", "★★★★", "★★★★★"])  # 계산 결과나 입력값을 이후 코드에서 다시 쓰기 위해 변수에 저장합니다.
st.write(f"이 음식점의 수준은: {level} 입니다.")  # 문자열, 숫자, 객체를 Streamlit 화면에 출력합니다.